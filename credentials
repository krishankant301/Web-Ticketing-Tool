https://github.com/krishankant301/Web-Ticketing-Tool

# Clone the repository
git clone https://github.com/krishankant301/Web-Ticketing-Tool
cd my-project

# Check branch
git branch

git pull origin main

# Create a new branch for your work
# Creates a branch named login-css
# Switches you into it
git checkout -b login-css


# Make changes to files
git add .
git commit -m "changed the css part"

# Push changes
git push -u origin login-css

# to merge the code to main
After this:
Go to GitHub → Open Pull Request → Friend reviews → Merge into main.

# You want to discard your local changes # Danger: this will delete your uncommitted work permanently.
git reset --hard
git pull origin main
